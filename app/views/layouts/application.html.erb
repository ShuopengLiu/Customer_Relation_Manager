<!DOCTYPE html>
<html>
  <head>
    <title>CustomerRelationManager</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.1/css/bulma.min.css">
  </head>

  <body>
    <div class="container">
          <header>
            <h1 class="title is-2"><%= link_to 'Customers', root_path %></h1>
            <h1 class="title is-2"><%= link_to 'Customers (Alphabetized)', alphabetized_path(@alphabetized) %></h1>
            <h1 class="title is-2"><%= link_to 'Customers (Missing Email)', missing_email_path(@missing_email) %></h1>
          </header>
      <%= yield %>
    </div>
  </body>
</html>
